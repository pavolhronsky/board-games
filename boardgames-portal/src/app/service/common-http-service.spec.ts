import { CommonHttpService } from './common-http-service';

describe('CommonHttpService', () => {
  it('should create an instance', () => {
    expect(new CommonHttpService()).toBeTruthy();
  });
});
